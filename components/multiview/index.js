import searchProducts from "./searchProducts.js";

const mainMultiview = {
	cells: [
		searchProducts,
		// importProducts,
		// priceManagement,
		// categories,
		// users,
		// confirmation
	],
};

export default mainMultiview;
